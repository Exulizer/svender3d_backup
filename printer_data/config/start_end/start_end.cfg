[gcode_macro START_PRINT]
gcode:
    UPDATE_DELAYED_GCODE ID=cooldown_motor_off DURATION=0 ; Timer stoppen
    # 0. Parameter einlesen
    {% set BED_TEMP = params.BED_TEMP|default(60)|float %}
    {% set EXTRUDER_TEMP = params.EXTRUDER_TEMP|default(205)|float %}
    {% set AREA_START = params.AREA_START|default("0,0") %}
    {% set AREA_END = params.AREA_END|default("0,0") %}
    _SET_TIMELAPSE_SETUP
    # 1. Bett aufheizen & Düse auf sichere 140°C Standby bringen
    M117 Warming up...
    M140 S{BED_TEMP}                ; Bett-Heizung starten
    M104 S140                       ; Düse auf 140°C Standby (verhindert Verkohlen)
    M190 S{BED_TEMP}                ; Warten bis Bett VOLL aufgeheizt ist
    
   # --- NEU: HEAT SOAK PAUSE (2 MINUTEN) ---
    # --- NEU: HEAT SOAK COUNTDOWN (2 MINUTEN) ---
    M117 Heat Soaking...
    RESPOND MSG="Heat Soak gestartet: 2 Minuten verbleiben..."
    G4 P30000                    ; Warte 30 Sekunden
    RESPOND MSG="Heat Soak: 90 Sekunden verbleiben..."
    M117 Soak: 90s...
    G4 P30000                    ; Warte 30 Sekunden
    RESPOND MSG="Heat Soak: 60 Sekunden verbleiben..."
    M117 Soak: 60s...
    G4 P30000                    ; Warte 30 Sekunden
    RESPOND MSG="Heat Soak: 30 Sekunden verbleiben..."
    M117 Soak: 30s...
    G4 P30000                    ; Warte 30 Sekunden
    RESPOND MSG="Heat Soak beendet. Starte Z-Tilt..."
    # ----------------------------------------
    # 2. Homing & Z-Tilt (Mechanik ist jetzt thermisch stabil)
    M117 Homing...
    G28 
    M117 Z-Tilt Adjust...
    Z_TILT_ADJUST                   ; Die Ausrichtung passiert jetzt nach der Wartezeit
    M117 Re-Homing Z...
    #G28 Z                           ; Z-Nullpunkt nach Ausrichtung neu setzen
    # 3. Meshing (Immer noch bei 140°C)
    M117 Meshing...
    BED_MESH_CLEAR
    BED_MESH_CALIBRATE AREA_START={AREA_START} AREA_END={AREA_END}
    BED_MESH_PROFILE LOAD=default  ; Stellt sicher, dass das frisch gemessene Mesh aktiv ist
    # --- SKEW CORRECTION ---
    SET_SKEW XY=69.93,70.03,49.85
    RESPOND MSG="Skew Correction wurde erfolgreich geladen."
    # 4. Parken & Finales Aufheizen
    # 4. Parken in der Nähe des Objekts & Finales Aufheizen
    G90                       
    
    # DYNAMISCHE POSITION BERECHNEN:
    # AREA_START splitten (Format ist meist "X,Y")
    {% set obj_x = AREA_START.split(',')[0]|float %}
    {% set obj_y = AREA_START.split(',')[1]|float %}
    
    # Berechne Startpunkt: 10mm links neben dem Objekt. 
    # Wenn das Objekt ganz links steht (obj_x < 15), starten wir fix bei X=5.
    {% set prime_x = [obj_x - 10, 5]|max %}
    {% set prime_y = obj_y %}
    
    # Fahre 2mm über die berechnete Startposition
    G1 X{prime_x} Y{prime_y} Z2.0 F5000           
    
    M117 Final Heating...
    M109 S{EXTRUDER_TEMP}           ; Auf volle Zieltemperatur aufheizen
    
    # 5. Bereitmachen für Prime Line (direkt am Objekt)
    G92 E0                          ; Extruder nullen
    G1 Z0.25 F300                   ; Düse flach aufs Bett absenken
    
    # 6. Aufruf deines Makros und Übergabe der dynamischen Position
    PRIME_LINE START_X={prime_x} START_Y={prime_y}

[gcode_macro _SAFE_BED_CALIBRATE]
description: Führt BED_MESH_CALIBRATE nur aus, wenn es von START_PRINT benoetigt wird
gcode:
    {% if printer["gcode_macro START_PRINT"].leveling_needed == 1 %}
        BED_MESH_CALIBRATE ADAPTIVE=1
    {% else %}
        # Wenn der Drucker bereits druckt, ignorieren wir den Befehl
        M117 Skipping Calibration...
    {% endif %}
    

[gcode_macro PRIME_LINE]
description: Dynamische 5cm Reinigung von links nach rechts mit Anti-Faden-Abriss
gcode:
    # Parameter laden
    {% set requested_x = params.START_X|default(5)|float %}
    {% set start_y = params.START_Y|default(5)|float %}
    
    # Sicherheits-Check: Startpunkt verschieben, falls rechts kein Platz für 50mm ist
    {% if requested_x + 50 > 220 %}
        {% set start_x = 220 - 50 %}
    {% else %}
        {% set start_x = requested_x %}
    {% endif %}
    
    # Ende ist immer 50mm rechts vom Start
    {% set end_x = start_x + 50 %}

    G90                                 ; Absolute Koordinaten
    G92 E0.0                            ; Extruder zurücksetzen
    
    # Falls der Startpunkt korrigiert werden musste, weich dorthin fahren
    G1 X{start_x} Y{start_y} F5000      
    
    # 1. Knackiger Druckaufbau (Squeeze) vor der Fahrt
    M117 Super Squeeze...
    G1 E1.5 F200                        ; 1.5mm Filament im Stand vorpressen
    G4 P1000                            ; 1 Sekunde warten, damit der Druck steht
    G92 E0.0                            
    
    # 2. Dynamic Prime Line (Zieht die 5cm Linie waagerecht von links nach rechts)
    M117 Purging...
    G1 X{end_x} Y{start_y} E3.0 F1200   ; 50mm Fahrt nach rechts mit 3mm Fluss
    
    # 3. ANTI-FADEN-MECHANIK (Absolut sauberer Abriss)
    G92 E0.0                            ; Extruder zurücksetzen
    G1 Z3.0 E-0.8 F3000                 ; Zieht Z auf 3mm hoch UND macht gleichzeitig 0.8mm Retract!
    
    # Richtungsentscheidung für den Wipe (Weg vom Bauteil)
    {% set wipe_y = start_y + 15 if start_y < 15 else start_y - 15 %}
    G1 Y{wipe_y} F6000                  ; Schneller 15mm Ruck reißt den Faden sauber ab
    
    # 4. Sichere Freifahrt zum Druckstart
    G1 Z2.0 F5000.0                     ; Bleibt auf sicheren 2mm Höhe während der Fahrt zum Objekt
    G92 E0.0                            ; Extruder final nullen für den Slicer

[gcode_macro END_PRINT]
gcode:
    # 1. ZUERST wegfahren
    G91                             ; Relative Positionierung
    G1 E-2 F2700                    ; Retract
    G1 Z10 F3000                    ; Hebt die Z-Achse um 10mm an
    G90                             ; Absolute Positionierung
    G1 X10 Y210 F5000               ; Deine Parkposition
    SET_SKEW CLEAR=1                
    
    # 2. Heizungen aus & Bauteillüfter AN zum schnellen Abkühlen
    M140 S0                         ; Bett ausschalten
    M104 S0                         ; Extruder ausschalten
    M106 S255                       ; Bauteillüfter auf 100%
    
    # 3. Statusmeldung & Video
    M117 Video & Abkuehlen...
    RESPOND MSG="Druck beendet. Starte Video-Rendering und Abkühlphase."
    TIMELAPSE_RENDER

    # 4. Warten bis Hotend unter 50°C ist
    # Das Makro pausiert hier so lange, bis die 50 Grad erreicht sind.
    M109 R50                        

    # 5. ALLES AUS (JETZT gehen Bauteillüfter und Motoren aus)
    M107                            ; Bauteillüfter AUS
    M84                             ; Motoren AUS
    M117 Standby
    

######################################################################
# Beeper
######################################################################

# M300 : Play tone. Beeper support, as commonly found on usual LCD
# displays (i.e. RepRapDiscount 2004 Smart Controller, RepRapDiscount
# 12864 Full Graphic). This defines a custom I/O pin and a custom
# GCODE macro.  Usage:
#   M300 [P<ms>] [S<Hz>]
#   P is the tone duration, S the tone frequency.
# The frequency won't be pitch perfect.

##[output_pin BEEPER_pin]
##pin: EXP1_1
#   Beeper pin. This parameter must be provided.
#   ar37 is the default RAMPS/MKS pin.
##pwm: True
#   A piezo beeper needs a PWM signal, a DC buzzer doesn't.
##value: 0
#   Silent at power on, set to 1 if active low.
##shutdown_value: 0
#   Disable at emergency shutdown (no PWM would be available anyway).
##cycle_time: 0.001
#   Default PWM frequency : 0.001 = 1ms will give a tone of 1kHz
#   Although not pitch perfect.

[gcode_macro M300]
gcode:
    # Use a default 1kHz tone if S is omitted.
    {% set S = params.S|default(1000)|int %}
    # Use a 10ms duration is P is omitted.
    {% set P = params.P|default(100)|int %}
    SET_PIN PIN=BEEPER_pin VALUE=0.5 CYCLE_TIME={ 1.0/S if S > 0 else 1 }
    G4 P{P}
    SET_PIN PIN=BEEPER_pin VALUE=0


######################################################################
# Filament Change
######################################################################

# M600: Filament Change. This macro will pause the printer, move the
# tool to the change position, and retract the filament 50mm. Adjust
# the retraction settings for your own extruder. After filament has
# been changed, the print can be resumed from its previous position
# with the "RESUME" gcode.

[pause_resume]

[gcode_macro M600]
gcode:
    {% set X = params.X|default(50)|float %}
    {% set Y = params.Y|default(0)|float %}
    {% set Z = params.Z|default(10)|float %}
    SAVE_GCODE_STATE NAME=M600_state
    PAUSE
    G91
    G1 E-.8 F2700
    G1 Z{Z}
    G90
    G1 X{X} Y{Y} F3000
    G91
    G1 E-50 F1000
    RESTORE_GCODE_STATE NAME=M600_state
